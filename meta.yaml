{% set name = "hole-filling-liepa" %}
{% set version = "0.0.3" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
#  git_rev: v0.0.1
#  git_url: https://github.com/russelmann/hole-filling-liepa.git
#  url: https://github.com/russelmann/hole-filling-liepa/archive/v{{ version }}.tar.gz
  path: .

build:
  number: 0
  
requirements:
  build:
    - cmake
  host:
    - python
  run:
    - python
    - numpy

test:
  imports:
    - hole-filling-liepa

about:
  home: https://github.com/russelmann/hole-filling-liepa
  license: MIT
  license_family: MIT
  summary: Hole filling algorithm by P. Liepa.
